"""LLM prompts for the Claw tiered memory system."""

CORE_MEMORY_UPDATE_PROMPT = """\
你是一个记忆管理系统。根据当前的核心记忆文档和最近的对话内容，更新核心记忆。

## 当前 MEMORY.md
{current_memory_md}

## 最近对话（第 {start_turn} 轮 ~ 第 {end_turn} 轮）
{recent_turns}

## 更新规则
根据最近对话中的新信息更新 MEMORY.md：
- 提炼用户偏好、关键事实、重要决策
- 如果新信息与旧条目冲突，用新信息替换旧条目
- 删除已过时或不再相关的信息
- 文档总长度控制在 10KB 以内
- 使用结构化 Markdown，分节清晰
- 不要包含原始对话内容——只保留提炼后的洞察
- 使用与用户对话相同的语言（中文对话用中文记忆，英文对话用英文记忆）

直接输出完整的更新后 MEMORY.md 内容，不要加任何包装或说明。"""

CORE_MEMORY_INIT_PROMPT = """\
你是一个记忆管理系统。根据以下对话内容，创建一份核心记忆文档。

## 对话内容
{recent_turns}

## 创建规则
从对话中提炼关键信息，生成结构化的 MEMORY.md：
- **用户画像**：用户的偏好、背景、使用习惯
- **关键决策**：对话中达成的重要结论或决定
- **核心知识点**：讨论过的重要概念和事实
- **活跃话题**：当前正在进行的讨论主题

使用与用户对话相同的语言。文档控制在 10KB 以内。
直接输出 MEMORY.md 内容，不要加任何包装或说明。"""

TURN_COMPRESS_PROMPT = """\
用一句话概括以下问答的核心信息交换：
问：{question}
答：{answer}

直接输出一句话摘要，不要加前缀。"""
